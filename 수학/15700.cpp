#include <iostream>
using namespace std;
typedef long long ll;

void input() {
	ll N, M;
	cin >> N >> M;
	cout << (N * M) / 2LL << '\n';
}

int main() {
	cin.tie(NULL); cout.tie(NULL);
	ios::sync_with_stdio(false);

	input();

	return 0;
}