#include <iostream>
using namespace std;
typedef long long ll;

ll x, y, z;

void input() {
	cin >> x >> y >> z;
	cout << x + y + z << '\n';
}

int main() {
	cin.tie(NULL); cout.tie(NULL);
	ios::sync_with_stdio(false);

	input();

	return 0;
}